### Category Management (`/api/categories`) 

| Scenario ID | Description / Purpose | Request Type & Endpoint | Request Body (Payload / Params) | Expected Result | Actual Result | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-CAT-01** | Creating Root (Parent) Category | `POST /api/categories` | `{ "name": "Literature & Fiction", "parentId": null }` | 201 Created or 200 OK. The response should return a unique ID (e.g., 1). The category must be created in the DB with correct information. | 201 Created response was received. The category was created successfully, a unique ID was generated, and DB verification was successful. | **PASS** |
| **TC-CAT-02** | Creating Child Category | `POST /api/categories` | `{ "name": "Turkish Literature TOP 50", "parentId": 1 }` | 201 Created. The response should return a new ID, and the parentId should be verified as 1. It must be added to the DB. | 201 Created response was received. The child category was created successfully, linked to the correct parent category (parentId: 1), and saved to the DB. | **PASS** |
| **TC-CAT-03** | Creating Child Category with Non-Existing Parent ID | `POST /api/categories` | `{ "name": "manga", "parentId": 999 }` | 400 Bad Request or 404 Not Found (Depends on business logic, BusinessException should be triggered). | 404 Not Found response was received and BusinessException was triggered successfully. | **PASS** |
| **TC-CAT-04** | Root Category Name Conflict (Exact Same Name) | `POST /api/categories` | **1st Request:** `{ "name": "Children's Books", "parentId": null }`<br>**2nd Request:** `{ "name": "Children's Books", "parentId": null }` | Should return 400 Bad Request. The error message should clearly say *"Category with name 'Children's Books' already exists"*. | 201 Created response was received and the duplicate data was saved to the DB. The expected error message was not received. | **FAIL** |
| **TC-CAT-05** | Child Category Name Conflict Under the Same Parent | `POST /api/categories` | **1st Request:** `{ "name": "Storybooks", "parentId": the ID of Children's Books }`<br>**2nd Request:** `{ "name": "Storybooks", "parentId": the ID of Children's Books }` | Should return 400 Bad Request. The system should not allow two "Storybooks" categories under the same parent. The error message should be clear. | 201 Created was returned and data was saved to the DB. The expected error message was not received. | **FAIL** |
| **TC-CAT-06** | Getting Existing Category by ID | `GET /api/categories/1` | Path Variable: `id = 1` | Should return 200 OK. The response should include id: 1 and name: "Literature & Fiction" without any missing data. | 200 OK response was received. id: 1 and name: "Literature & Fiction" were fully verified in the response body. | **PASS** |
| **TC-CAT-07** | Listing All Categories | `GET /api/categories` | None | Should return 200 OK. The list must not be empty and must include the data added in TC-CAT-01 and 02. | 200 OK response was received. Verified that the list is not empty and contains the test data successfully. | **PASS** |
| **TC-CAT-08** | Querying Non-Existing Category | `GET /api/categories/999` | Category with ID 999 does not exist in DB. | Should return 404 Not Found. The message field in the response should contain "Category not found". | 404 Not Found response was received. Verified that the error message is "Category not found". | **PASS** |
| **TC-CAT-09** | Sending Request with Invalid Data Type (Validation) | `GET /api/categories/abc` | Sending a string (`abc`) instead of an integer ID. | Should return 400 Bad Request. The system should not crash or throw a Java error (500). | When invalid input is sent, the system does not save the data. However, the Global Exception Handling mechanism returns a generic 500 Internal Server Error. The client-side format error is wrapped in an `"Unexpected error occurred"` message.<br><br>**Response Body:**<br>`{`<br>&nbsp;&nbsp;`"status": 500,`<br>&nbsp;&nbsp;`"error": "Internal Server Error",`<br>&nbsp;&nbsp;`"message": "Unexpected error occurred",`<br>&nbsp;&nbsp;`"path": "/api/categories/abc",`<br>&nbsp;&nbsp;`"timestamp": "2026-05-30T20:49:54"`<br>`}` | **FAIL** |
| **TC-CAT-10** | Updating Category Name | `PUT /api/categories/1` | Path Variable: `id = 1`<br>Body: `{ "name": "Literature" }` | Should return 200 OK. When a GET request is made to `/api/categories/1`, the updated name should be visible. It must change in the DB too. | 200 OK response was received. Controlled with a GET request, and verified that the name field was updated in the DB. | **PASS** |
| **TC-CAT-11** | Deleting a Category from the System | `DELETE /api/categories/2` | Path Variable: `id = 2` (Child Category - Turkish Literature TOP 50) | Should return 200 OK or 204 No Content. | 204 No Content response was received and the deletion was verified in the DB. | **PASS** |
| **TC-CAT-12** | Trying to Delete a Non-Existing Category | `DELETE /api/categories/2` | ID does not exist in the database. | Should match 404 Not Found standards. | 404 Not Found response was received. It is compatible with the standard API error format. | **PASS** |
| **TC-CAT-13** | Required Field Control: Missing 'name' Field | `POST /api/categories` | `{ "parentId": null }` *(name field is missing)* | Should return 400 Bad Request. The standard error format should show which field is missing in the message part. | 500 Internal Server Error response was received. The global error handler wrapped the missing field validation into a generic "Unexpected error occurred" message. | **FAIL** |
| **TC-CAT-14** | Empty Value Control: Empty String for 'name' | `POST /api/categories` | `{ "name": "", "parentId": null }` | Should return 400 Bad Request. An empty category name should not be saved to the DB, and a validation message should be returned. | 500 Internal Server Error response was received. Instead of validating the input, the system threw a generic server error for the empty string. | **FAIL** |
| **TC-CAT-15** | Wrong Data Type: String for 'parentId' | `POST /api/categories` | `{ "name": "Books", "parentId": "stringID" }` | Should return 400 Bad Request. The system should not throw an unhandled error (500) and should show a proper validation message. | 500 Internal Server Error response was received. The type mismatch error fell into the generic error mechanism instead of being handled by BusinessException. | **FAIL** |
| **TC-CAT-16** | Boundary Value Analysis: Very Long Character Input | `POST /api/categories` | `{ "name": "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Aliquam nisi leo, malesuada porttitor justo ac, consectetur eleifend purus.", "parentId": null }` | 400 Bad Request (if there is a DB character limit) or the system should handle this properly. | 500 Internal Server Error response was received. When the character limit was exceeded, the system threw a generic 500 instead of converting the DB error to a 400 client error. | **FAIL** |
| **TC-CAT-17** | Sending Request with Empty Body | `POST /api/categories` | `{ }` or completely empty request body | Should return 400 Bad Request. The system should give a controlled "Request body is missing" validation error. | 500 Internal Server Error response was received. Instead of returning 400 Bad Request to the client, the system threw a generic 500 error. | **FAIL** |

---

### Summary of the Report
* **Total Cases:** 17
* **Passed:** 9
* **Failed:** 8

> **QA Note:** The "Happy Path" scenarios work perfectly (creating, updating, and deleting categories). However, the system fails and returns a `500 Internal Server Error` during negative testing and input validation. The Global Exception Handling mechanism needs to be improved by the development team to convert these server crashes into clean `400 Bad Request` messages.